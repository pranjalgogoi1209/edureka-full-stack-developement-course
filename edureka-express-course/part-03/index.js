/* 
 architecture of nginx
 master, worker, proxy cache, cache loader, cache manager
*/

/* 
    CORS
        same origin 
        cross origin 
            cross origin problem
*/

/* 
 properties of domain and url
    server type, host name, port, top-level domain, method/protocol 
*/

/* 
    handle cross origin requrest
        simple request
        request with credentials
        preflight request
*/

/* 
    MVC Architecture
        modal => It has data
        view 
        controller 
*/
